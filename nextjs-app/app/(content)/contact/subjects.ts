/** Shared between the server action (validation) and the form (options). */
export const SUBJECTS = [
  "Question about an order",
  "Personalization help",
  "Bulk / business order",
  "Problem with my item",
  "Something else",
] as const;
